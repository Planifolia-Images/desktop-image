name: enable-plymouth-theme
type: shell
commands:
  - plymouth-set-default-theme vanilla-bgrt
modules:
  - name: add-plymouth-hook
    type: shell
    commands:
      - sed -i "s|base udev btrfs|base udev plymouth btrfs|g" /etc/mkinitcpio.conf
